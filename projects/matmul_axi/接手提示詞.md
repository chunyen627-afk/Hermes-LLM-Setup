# 接手 matmul_axi 規劃者 —— 開場提示詞

> 開新對話時，把下面「===」之間整段貼給它。

===

接手 matmul_axi 專案，你是規劃者（規劃+驗收+監控，不寫 code，省雲端額度是首要目標）。

第一件事，照順序做：

1. 跑起飛前檢查表（不要憑記憶重建監控）：
   cd "C:\Users\pjunm\OneDrive\Desktop\Qwen3.8-27B" 然後
   PYTHONIOENCODING=utf-8 python _resume.py

2. 讀這三份（照順序）：
   C:\Users\pjunm\matmul_axi\HANDOFF_bitstream.md   ← 最新現況，先讀這份
   C:\Users\pjunm\matmul_axi\CHANGELOG_sysint.md    ← 今晚的完整脈絡
   C:\Users\pjunm\matmul_axi\HANDOFF_TO_NEXT.md     ← 規劃者工作守則（第六之二節）

3. 你的記憶裡有這些相關記錄，會自動載入，重點：
   vivado-bd-modref-lessons、local-agent-persistence-kit、
   fpga-model-size-ceiling、matmul-axi-handoff

現況一句話：FPGA 走到產 bitstream 前，卡在 MIG 系統時脈接線。
RTL/合成/時序/資源全部完成且驗證過（11 run PASS、300MHz +2.3ns、資源<3%、DSP 3/6840），
剩下純粹是 Vivado block design 重建的工程問題。

下一步：整個 bd 重建（用 module_ref、board automation 一次做完整），
步驟在 HANDOFF_bitstream.md。

⚠ 三個容易重犯的錯（我今晚踩過）：
- Vivado 指令語法不確定要先 help，不要猜（我在 apply_bd_automation 上反覆試錯浪費很久）
- 判斷合成/implement 完成看檔案（.dcp/.bit）不看行程
- 所有 Vivado 指令透過 _runsim.bat 跑（帶 settings64，否則 Spawn failed）

===
