# 踩過的坑

全部都是實際遇到並解決的，按嚴重程度排序。

---

## 🔴 1. llama.cpp 版本太舊 → stream 模式回空值

**症狀**：agent 跑很久完全沒動作，看起來像模型在思考。

**真相**：舊版（2026-06 前）的 build，非 stream 請求正常，**stream 請求回空的**。
Claude Code / Hermes 都只用 stream → 每次請求都收到空回應 → 觸發重試 →
重試 10 次，每次都重跑整個 prompt。

**怎麼確認**：
```json
{"subtype":"api_retry","error_status":null,"error":"unknown"}
```
`/slots` 看到 `n_prompt_tokens_processed` 停住不動，但 task 編號暴衝
（實測 1560 → 2471，900 多次請求全是白工）。

**解法**：用 b10435 或更新的 build。

---

## 🔴 2. chat template 的 raise_exception

**症狀**：HTTP 400，訊息是
```
Jinja Exception: System message must be at the beginning.
```

**原因**：Qwen3.8 官方 template 第 110 行有：
```jinja
{{- raise_exception('System message must be at the beginning.') }}
```
agent 框架會送多個 system/developer message → 直接爆。

**解法**：把那行改成當 user message 輸出：
```jinja
{{- '<|im_start|>user\n' + content + '<|im_end|>' + '\n' }}
```
本 repo 的 `qwen38_chat_template.jinja` 已修好，啟動帶 `--chat-template-file`。

---

## 🔴 3. Hermes 說 Connection error，但網路正常

**症狀**：
```
API call failed after 3 retries: Connection error.
```

**真相**：**這訊息會誤導**。Hermes 是 agent 框架，會檢查模型有沒有宣告
`tools` 能力。llama-server 的 `/v1/models` 只回：
```json
"capabilities": ["completion"]
```
沒有 `tools` → Hermes 拒用 → 報成連線錯誤。

**解法**：橋接器攔截 `/v1/models`，把回應改成：
```json
"capabilities": ["completion", "tools", "chat"]
```

---

## 🔴 4. 不要選 Ollama provider

Hermes 的 `--provider ollama` 走的是 **Ollama 雲端服務**，不是本機。

**怎麼確認**：`hermes doctor` 會顯示 `✓ ollama-cloud`。

**解法**：選 **LM Studio** provider，它打 `127.0.0.1:1234`，橋接器轉發那個 port。

---

## ⚠️ 5. context_length 兩邊不一致

Hermes 預設 `context_length: 262144`，但伺服器可能只有 176K。

| 情況 | 後果 |
|---|---|
| Hermes 設得**比伺服器大** | 一路塞到超過才發現，直接報錯或截斷 |
| Hermes 設得**比伺服器小** | 浪費可用空間 |

**解法**：用 `3-CHECK-CTX.bat <IP>` 查伺服器實際值，兩邊設一致。
同時開 `compression: enabled: true`，快滿時自動壓縮而不是撞牆。

---

## ⚠️ 6. Hermes 有兩個 config.yaml

```
C:\Users\<你>\.hermes\config.yaml              ← 舊的，Hermes 不讀
C:\Users\<你>\AppData\Local\hermes\config.yaml ← 實際使用的
```

**確認方法**：`hermes config path`

---

## ⚠️ 7. prompt cache 對 hybrid 架構失效

**症狀**：log 出現
```
forcing full prompt re-processing due to lack of cache data
(likely due to SWA or hybrid/recurrent memory)
```

**原因**：Qwen3.8 是 hybrid 架構（64 層裡 48 層是 linear attention），
llama.cpp 的 checkpoint 搜尋用 SWA 的 `pos_min` 判斷，但 recurrent 模型的
`pos_min` 永遠等於全長 → 檢查永遠失敗。
加上 `--checkpoint-min-step` 預設 8192 太大，一般對話輪次根本建不出 checkpoint。

**解法**（b10435 已有這些旗標）：
```
--swa-full  --checkpoint-min-step 0  --ctx-checkpoints 32  --cache-ram -1
```
實測 cache 從 0 → **命中率 95-97%**，第三輪耗時 100 秒 → 27.8 秒。

---

## ⚠️ 8. reasoning 參數的雷

**`--reasoning` 合法值只有 `on|off|auto`**，寫 `none` 會導致啟動失敗
（症狀：VRAM 停在 0，看起來像沒反應）。

**`--reasoning auto` 沒有上限**，模型會陷入思考迴圈：
`/slots` 的 `n_prompt_tokens_processed` 停住不動，但 `n_prompt_tokens` 一直漲
（那是它在自言自語）。實測跑了 18 分鐘零產出。

**必須明確設 budget**：
```
--reasoning on --reasoning-effort high --reasoning-budget 4096
```

⚠ 但 `--reasoning-budget` **對這個模型沒有硬約束力** —— budget 2048 照樣繞
18 分鐘。要完全避免就用 `--reasoning off`。

---

## ⚠️ 9. .bat 和 .ps1 的編碼要求相反

| 檔案 | 編碼 | 為什麼 |
|---|---|---|
| `.bat` | **UTF-8 無 BOM** | 有 BOM 的話 cmd 第一行會出錯 |
| `.ps1` | **UTF-8 有 BOM** | 沒 BOM 中文會亂碼 |

`.bat` 顯示中文亂碼 → 開頭加 `chcp 65001 >nul`。

---

## ⚠️ 10. bat 裡的多行 if 區塊會展開變數

**症狀**：一堆 `'xxx' 不是內部或外部命令`，而且環境變數沒設成功
（結果連到雲端而不是本地模型）。

**原因**：cmd 解析括號區塊時會提前展開變數，路徑裡的字元被拆開。

**解法**：用 `goto` 標籤取代多行 `if (...)`，避免中間變數，直接用 `%~dp0`。

---

## ⚠️ 11. Test-NetConnection 沒有 timeout

PowerShell 的 `Test-NetConnection` 遇到剛關閉的 port 會卡很久
（症狀：視窗停在「Waiting for response」）。

**解法**：改用有 `-TimeoutSec` 的 `Invoke-RestMethod` 直接試。

---

## ⚠️ 12. 排程啟動時 python 不在 PATH

`Start-Process -FilePath 'python'` 在排程環境下會找不到。

**解法**：用 `.bat` 包一層，裡面寫完整路徑，再由排程呼叫那個 bat。

---

## ⚠️ 13. 模型會抓到壞掉的 gcc

模型自己找編譯器時可能抓到 `C:/msys64/mingw64` 底下的
（缺 `libmpfr-6.dll`，是壞的），然後一直修不好。

**解法**：在 CLAUDE.md 寫明正確路徑，例如：
```
C:/Users/<你>/AppData/Local/Microsoft/WinGet/Packages/
BrechtSanders.WinLibs.POSIX.UCRT_*/mingw64/bin/gcc.exe
```

---

## 💡 14. Claude Code 的工具定義吃掉一半 context

不是坑，但影響很大：Claude Code 帶 36 個工具定義
（Cron、Worktree、DesignSync、TaskCreate…），對本地模型完全沒用，
卻佔了 16K+ tokens。

**這就是為什麼同一個模型在 Hermes 下表現好得多。**

如果非要用 Claude Code：
- `--strict-mcp-config` 只載必要的 MCP
- 規則拆成 Skills 按需載入（常駐從 9.6K 降到 1.7K）

---

## 🔴 15. Hermes 桌面版出廠預設是 Google Vertex（2026-08-20）

官方安裝程式（`Hermes-Setup.exe`）裝完的 `config.yaml` 長這樣：

```yaml
model:
  default: google/gemini-3.1-pro-preview-customtools
  provider: vertex          # ← 這個
  base_url: ''
```

**症狀**：送訊息噴

```
HTTP 400: Malformed publisher model (`model`: 'qwen38_mtp')
for the 'openapi' request endpoint ID; expected 'publisher/model'
```

那是 Vertex 的格式要求 —— 代表請求打到 `aiplatform.googleapis.com` 而不是本地。

**不是殘留設定，重裝幾次都會這樣。** 裝完第一件事：

```
hermes config set model.provider lmstudio
hermes config set model.default qwen38_mtp
hermes config set model.base_url http://127.0.0.1:1234/v1
hermes config set model.context_length 176000
hermes config set display.language zh-hant
```

裝完會跳 setup wizard —— **按 Ctrl+C 跳過**，選 1（Nous Portal）會把本地設定覆蓋掉。

---

## 🔴 16. session 會把 provider 凍結在自己的記錄裡（最難查的一個）

`state.db` 的 `sessions.model_config` 欄位存著**該對話建立當下**的
provider / base_url。**改 `config.yaml` 不會影響已存在的對話。**

**症狀**：明明 config 改好了，在舊對話裡送訊息照樣打雲端。
表現出來就是「新開的對話正常、舊對話一直錯」。

查法：

```python
import sqlite3
c = sqlite3.connect(r'C:\Users\<你>\AppData\Local\hermes\state.db')
c.execute('SELECT id, model, model_config FROM sessions ORDER BY started_at DESC LIMIT 5').fetchall()
```

**解法**：清掉那筆的 `provider` / `base_url` 鍵，或**直接開新對話**。

**切換模型後也一定要開新對話**，舊的不會跟著換。

---

## 🔴 17. 授權逾時會讓模型「不准驗證、直接交件」

`approvals` 沒關的話，模型跑 terminal（編譯、測試）會等你按確認，
**60 秒沒回應就被擋掉**：

```
Tool terminal returned error (60.05s): {"error": "BLOCKED: Command timed out
without user response. The user has NOT consented to this action.
Do NOT retry this command, do NOT rephrase it..."}
```

後果比「慢」嚴重：模型被明確告知不准重試，只好**跳過驗證直接產出程式碼**。
長時間任務（跑一整晚那種）你不可能一直守著按確認，等於全程沒有編譯驗證。

**全自動授權設定**：

```
hermes config set approvals.mode off
hermes config set approvals.mcp_reload_confirm false
hermes config set approvals.destructive_slash_confirm false
hermes config set agent.subagent_auto_approve true
```

`approvals.cron_mode` 建議維持 `deny` —— 那是讓排程任務在你不在時
執行任意指令，風險等級不同。

**改完要重啟 Hermes 才生效。**

---

## ⚠️ 18. 壓縮預設是關的，門檻也不是你以為的 80%

新裝的預設值：

```yaml
compression:
  enabled: false      # 關的
  threshold: 0.5      # 50%，不是 80%
  target_ratio: 0.2
```

長對話會一路漲到 ctx 上限然後爆掉。建議：

```
hermes config set compression.enabled true
hermes config set compression.threshold 0.8
```

0.8 = 180224 × 0.8 = **144,179 tokens 觸發**，還留 36K 讓壓縮過程跑完。

**代價**：觸發那一次要等 2-5 分鐘 —— 模型要讀完整個 context 寫摘要，
然後 KV cache 全部重建。監看上會看到 ctx 從 144K 突然掉到 36K。

---

## ⚠️ 19. 別用 `hermes update`（git 版），用官方安裝檔

`hermes update` 拉 git 主線，今天造成一連串問題：

- npm 版本卡在不支援區間（要 `<11.10.0 || >=11.17.0`）
- venv 被弄壞：`click` 套件只剩 `__pycache__`、後來連 `pip` 都不見
  → `AttributeError: module 'click' has no attribute 'Choice'`
  → 桌面版 `Hermes couldn't start / backend exited before it became ready`
- **`Repair install` 修不了這種結構性損壞**
- 本地殘留的舊檔會擋新模組（`lib/chat-messages.ts` 擋住拆分後的 `lib/chat-messages/`）

**解法**：`https://hermes-assets.nousresearch.com/Hermes-Setup.exe` 重裝。
重裝**不會**動 `config.yaml`、`state.db`（聊天記錄）、skills。

---

## 💡 20. 監看要能分辨「冷啟動重算」和「搶 slot」

兩者症狀一樣（快取低 + 重算大），但意義完全不同：

| | 重算量 vs 總 prompt | 意義 |
|---|---|---|
| 冷啟動 | 重算 ≈ 全部（>90%）| 正常，只發生一次 |
| 搶 slot | 重算 < 90% | 有第二個對話在洗 KV，會反覆發生 |

示警條件要加 `proc < used * 0.9`，否則每次重啟都誤報。

另外 **llama-server 閒置時 `n_prompt_tokens_cache` 會歸零**，
算出來快取 0% 但那不是問題 —— 示警要先檢查 `is_processing`。

---

## 🔴 21. `agent.max_turns` 預設 60 —— 長任務會反覆被腰斬

**這是今天找到影響最大的瓶頸。**

一個五小時的韌體專案用掉 **186 次工具呼叫**，但預設上限是 60。
撞到上限不是「停下來」，是被強制中斷：

```
[user] You've reached the maximum number of tool-calling iterations allowed.
       Please provide a final response summarizing what you've found and
       accomplished so far, without calling any more tools.
```

它得停下手邊的事去寫總結，下一輪還要重新理解自己做到哪 ——
**那些總結本身又吃掉 context**。表現出來就是「跑一整晚做不完」，
但根本不是模型慢。

```
hermes config set agent.max_turns 500
hermes config set code_execution.max_tool_calls 500
```

判斷你撞到沒有：

```python
import sqlite3
c = sqlite3.connect(r'C:\Users\<你>\AppData\Local\hermes\state.db')
c.execute("SELECT COUNT(*) FROM messages WHERE session_id=? AND role='tool'",
          ('<session_id>',)).fetchone()
```

數字遠大於 `max_turns` 就是一直在撞。

---

## 🔴 22. 壓縮用本地模型會超時失敗（壓了等於沒壓）

`auxiliary.compression.provider: auto` 代表「用主模型」，
也就是叫本地 27B 讀完 146K 再寫摘要。光 prefill 就要五分鐘，
但 Hermes 的判定是「**120 秒沒吐出第一個 token 就算沒進度**」：

```
15:31:19  context compression started: messages=215 tokens=~146,255 model=qwen38_mtp
15:33:19  Context compression made no progress for 120.0s — continuing without
15:34:22  Compression commit cancelled before session mutation
```

取消後 `sessions.compacted` 全是 0，ctx 繼續漲到上限。
**設定看起來是開的，實際上完全沒作用。**

解法是把壓縮交給雲端小模型 —— 摘要不涉及隱私，也不該佔本地 GPU：

```
hermes config set auxiliary.compression.provider gemini
hermes config set auxiliary.compression.model gemini-2.5-flash
hermes config set auxiliary.compression.api_key <你的 key>
hermes config set auxiliary.compression.timeout 300
```

Gemini 免費層（[aistudio.google.com/apikey](https://aistudio.google.com/apikey)）
讀 146K 幾秒就回。**主模型仍然走本地，只有壓縮和視覺出去。**

驗證有沒有真的壓過：

```sql
SELECT COUNT(*) FROM messages WHERE session_id=? AND compacted=1
```

---

## ⚠️ 23. 視覺工具沒設就打本地模型，然後 500

`auxiliary.vision.provider: auto` 同樣是「用主模型」，
但 Qwen3.8 不支援視覺 → llama-server 回 500 → 橋接器轉成 502：

```
Tool vision_analyze returned error: Error code: 502 - {'error': 'upstream'}
```

模型想把 framebuffer 存成 PNG 檢查版面（純數學測試看不出上下顛倒
這類錯誤），卻看不到。同樣交給 Gemini：

```
hermes config set auxiliary.vision.provider gemini
hermes config set auxiliary.vision.model gemini-2.5-flash
hermes config set auxiliary.vision.api_key <你的 key>
```

⚠ 用明確版本號 `gemini-2.5-flash`，不要用 `gemini-flash-latest` ——
後者常常 `503 high demand`。

---

## 💡 24. 三個設定一起看才是完整的長任務配置

今天證實這三個限制會同時發生，而且都不是模型能力問題：

| 設定 | 預設值 | 不改的後果 |
|---|---|---|
| `agent.max_turns` | 60 | 每 60 次工具呼叫被腰斬一次 |
| `approvals.mode` | 視版本 | 編譯驗證等你按確認，60 秒後 BLOCKED |
| `auxiliary.compression.provider` | auto | 壓縮超時取消，ctx 撞上限 |

**三個都沒改的情況下跑長任務，會得到「本地模型做不完」的錯誤結論。**
先把設定調對，再評估能力。

另外：**對話會被壓縮，檔案不會**。要模型在每個階段把決策寫進
`docs/`，那是壓縮壓不掉的資產 —— 摘要的摘要會越來越糊，
但磁碟上的筆記不會。

---

## 🔴 25. 開 2 個 slot 要重算 tensor-split（2026-08-22）

`--parallel 2` 之後每個 slot 都要一份自己的 MTP context，
原本剛好塞滿的分配會爆：

```
allocating 864.00 MiB on device 2: cudaMalloc failed: out of memory
failed to create MTP context
srv load_model: failed to create MTP context
```

單 slot 時 `--tensor-split 6,12,13` 讓 GPU2 用到 11,934/12,288（只剩 354MB），
第二個 slot 的 MTP context 就進不去了。

**解法：把負擔往還有空間的卡移** —— `6,12,13` → **`7,12,11`**。

實測邊界（32GB = 3070 8G + 3060 12G ×2，Q4 模型 16.7GB）：

| 配置 | 結果 |
|---|---|
| 2 slot × 120K (`--ctx-size 245760`) | ✅ VRAM 28.6/32GB |
| 2 slot × 140K | ❌ 爆 |
| 2 slot × 120K + mmproj | ❌ 爆 |
| 2 slot × 96K + mmproj | ✅ |

**為什麼要開 2 slot**：自己跟家人同時用不會互搶 KV cache。
單 slot 交錯使用會慢 13 倍（見「單機集中」那條）。

---

## 💡 26. 沒有 mmproj 也能讓模型看圖 —— 在橋接器攔截

llama-server 掛 `--mmproj` 會多吃約 0.9GB VRAM，2 slot 配置下直接爆。
但圖片其實不必給本地模型看：

```
手機送圖 → 橋接器攔下 → Gemini 轉成文字描述 → 本地模型只收到純文字
```

`hermes_bridge.py` 加兩個函式：

```python
def _strip_images(body):
    if not body or b'image_url' not in body:   # 沒圖零成本，不解析 JSON
        return body, 0
    # 把 content 陣列裡的 image_url 換成 Gemini 的文字描述
```

在 `_fwd()` 轉發前呼叫。**本地模型完全不知道有圖這回事。**

實測：`123+45=168` 的截圖，模型正確答出 168，全程 23 秒
（Gemini 看圖 5 秒 + 本地生成 18 秒）。VRAM 零成本。

金鑰用 `GEMINI_API_KEY` 環境變數，免費 key 在
[aistudio.google.com/apikey](https://aistudio.google.com/apikey)。

**⚠ 用明確版本號** `gemini-2.5-flash`，不要用 `gemini-flash-latest`
（後者常 `503 high demand`）。

---

## ⚠️ 27. 換無審查版模型時，切換器會誤判「已經在跑」

abliterated 版跟官方版的 `--alias` 通常一樣（都設 `qwen38_mtp`），
而切換器是比對 alias 判斷要不要重載：

```powershell
if ($cur.models[0].name -eq $sel.Alias) {
    Write-Host "已經在跑了，不用切換"; exit 0
}
```

換了 `.gguf` 檔路徑但 alias 沒變 → 它以為不用動 → 還是舊模型。

**解法**：換檔案後先 `Get-Process llama-server | Stop-Process -Force` 再跑切換器。

實測可用的無審查版：
`HauhauCS/Qwen3.8-27B-Uncensored-HauhauCS-Aggressive-MTP-GGUF`
的 `Q4_K_P`（16.69GB，比官方 UD-Q4_K_XL 小 0.4GB）。
repo 名字就標 MTP，abliteration 沒動 MTP head 與 vision tower。

⚠ **MLX 格式不能用** —— 那是 Apple Silicon 專用，llama.cpp 只吃 GGUF。

---

## 💡 28. 非對稱多卡只有 llama.cpp 撐得住

查過 vLLM / SGLang / Ollama / TensorRT-LLM，結論是換框架這條路是死的：

| 框架 | 8+12+12 非對稱 | MTP |
|---|---|---|
| **llama.cpp** | ✅ `--tensor-split` 精確控制 | ✅ |
| vLLM | ❌ 對稱假設，只認 8×3=24GB | ❌ |
| SGLang | ❌ 同上 | ❌ |
| Ollama | ⚠️ 自動分配不可控 | ❌ 無此參數 |

vLLM/SGLang 的張量平行假設所有卡一樣大，會以最小的為準
（浪費 8GB），而且非對稱張量平行本身有 8-49% 吞吐衰退。
那些是為資料中心設計的 —— 同型號 GPU 整齊排列。
**非對稱消費級多卡是 llama.cpp 的守備範圍。**

---

## 💡 29. ctx 膨脹太快 → 裝 Serena 用符號查詢取代讀整檔

一個韌體專案跑到 175K 觸發壓縮，壓縮後模型忘記先前的決定。
查下來最大宗的浪費是**反覆 Read 整個檔案**。

**Serena** 是 LSP 符號索引的 MCP server，讓模型只取需要的符號：

| 想做什麼 | 用這個 | 取代 |
|---|---|---|
| 看檔案有什麼函式 | `get_symbols_overview` | Read 整檔 |
| 看某個函式內容 | `find_symbol(include_body=True)` | Read 整檔再找 |
| 改動會影響誰 | `find_referencing_symbols` | grep 全專案 |
| 換掉整個函式 | `replace_symbol_body` | Read 全檔再 Write |

安裝（`uvx` 就在 Hermes 自帶的 `AppData\Local\hermes\bin\`）：

```bash
hermes mcp add serena \
  --command "<LOCALAPPDATA>\hermes\bin\uvx.exe" \
  --connect-timeout 180 \
  --args --from "git+https://github.com/oraios/serena" serena \
         start-mcp-server --context ide-assistant --open-web-dashboard False
```

⚠ `--context` 用 `ide-assistant`，Claude Code 的 `claude-code` 是另一個。

**只啟用 8 個工具**（預設 24 個）—— 工具定義每次請求都帶著，
24 個會吃 3-5K context。在 config.yaml 的 `mcp_servers.serena` 底下加：

```yaml
    enabled_tools:
      - get_symbols_overview
      - find_symbol
      - find_referencing_symbols
      - find_declaration
      - find_implementations
      - replace_symbol_body
      - get_diagnostics_for_file
      - activate_project
```

**為什麼選 Serena 而不是向量方案**：

| 路線 | 判斷 |
|---|---|
| **LSP 符號**（Serena）| ✅ 編譯器級精確、**不用建索引**、不吃 VRAM |
| 向量嵌入（ChunkHound 等）| ❌ 無法確定誰呼叫誰、唯讀、要跑 embedding 模型吃 VRAM |
| 純圖形（CodeGraphContext）| ❌ 語意召回差、排序弱 |

「不用建索引」在專案結構常變、VRAM 又緊的機器上特別重要。

⚠ **但符號查詢不是萬用** —— 實測顯示對「這專案大概在幹嘛」這種模糊探索，
一直來回符號查詢**反而貴 4 倍、慢 60%**。
已知道要找什麼（改某函式、追呼叫鏈）才是它的主場；
第一次接觸專案還是先看 README 和目錄結構建立地圖。

**光裝不夠，要寫進規則** —— 模型得知道「什麼時候該用 `find_symbol`
而不是 Read」。規則章節見下一條。

---

## ⚠️ 30. CLAUDE.md 是產出物，改它會被蓋掉

`hermes/CLAUDE.md` 不是來源檔，它是 `_extract_rules.py` 從
`remote-station/gui/app.py` 裡的 `WIN_RULES` 字串產生的。

今天把省 context 的規則寫進 CLAUDE.md，跑了一次同步就被還原成舊版
（37661 bytes），白做。

**正確流程**：

```
改 app.py 的 WIN_RULES  →  python _extract_rules.py  →  產生 CLAUDE.md
```

`WIN_RULES` 的格式是每行一個 Python 字串常數：

```python
    WIN_RULES = (
        "## 章節標題\n"
        "- 條目一\n"
        "- 條目二\n"
    )
```

插入新章節時要保留原本的縮排，而且字串裡的引號要跳脫。
改完一定要 `python -c "import ast; ast.parse(open('app.py',encoding='utf-8').read())"` 驗語法。
